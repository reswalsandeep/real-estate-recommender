# Core data stack (matches versions already present in the base interpreter)
pandas>=2.2,<3
numpy>=2.0,<3

# Modelling — added for the Section 5 model-training layer
scikit-learn>=1.4,<2   # TargetEncoder with internal cross-fitting needs >=1.3
joblib>=1.3

# Explainability — src/models/explain.py (Section 5 point 4)
shap>=0.44
matplotlib>=3.7   # SHAP plots + the committed sector-price map (Section 4)

# Sector price map — src/features/geo.py (Section 4)
plotly>=5.24,<6   # scatter_mapbox (dropped in Plotly 7) is the only OSM map that renders inside Streamlit here — scatter_map / MapLibre paints blank

# Streamlit app — app/ (Section 12)
streamlit>=1.40   # st.navigation / st.Page need >=1.36
wordcloud>=1.9    # amenities word cloud on the Market Insights page

# Notebook execution for notebooks/08_model_training.ipynb
jupyter>=1.0
nbconvert>=7
ipykernel>=6

# Test suite — tests/ (focused parser / logic checks, not full coverage)
pytest>=8
