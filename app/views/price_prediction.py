"""Price Prediction page — stub.

Will take a property's features and return `price_pipeline.pkl`'s estimate,
with the per-prediction SHAP breakdown from `PriceExplainer`.
"""

import streamlit as st


def render() -> None:
    st.title("💰 Price Prediction")
    st.info("Not built yet — coming after the skeleton is confirmed.")
